# JSDM Studio 运行结果文件详细解释

本手册解释 JSDM Studio 运行后生成的每个主要文件是什么、里面的行列是什么意思、应该怎么看、哪些可以写进论文。

运行成功后，结果通常保存在：

```text
output/hmsc_日期时间/
```

例如：

```text
output/hmsc_20260517_153000/
```

---

# 1. 顶层文件

## 1.1 used_config.yml

### 是什么？

本次运行使用的完整参数配置文件。

### 里面有什么？

通常包括：

```text
输入文件路径
distr
XFormula
TrFormula
随机效应设置
MCMC 参数
输出选项
seed
```

### 怎么用？

这是保证可重复性的关键文件。别人可以根据它知道你当时如何运行模型。

### 论文意义

建议在补充材料中保存或上传。

---

## 1.2 RUN_COMPLETE.txt

### 是什么？

运行完成标记。

### 里面有什么？

通常是运行时间、完成状态或简单说明。

### 怎么用？

确认程序确实跑完，而不是中途停止。

---

## 1.3 JSDM_Studio_report.html / HmscGUI_report.html

### 是什么？

自动生成的 HTML 报告或结果索引。

### 里面有什么？

```text
运行摘要
输入数据维度
模型设置
输出文件链接或说明
```

### 怎么用？

适合快速浏览结果，也适合给合作者看。

---

# 2. inputs/ 文件夹

## 2.1 原始输入文件副本

### 是什么？

程序会复制本次上传的输入文件。

可能包括：

```text
Y.csv
XData.csv
TrData.csv
studyDesign.csv
coordinates.csv
phyloTree
```

### 为什么重要？

保证以后能知道这次模型到底用了哪一版数据。

### 行列含义

和输入文件一致：

```text
Y：行 = 样方，列 = 物种
XData：行 = 样方，列 = 环境变量
TrData：行 = 物种，列 = 性状
studyDesign：行 = 样方，列 = 分组变量
coordinates：行 = 样方，列 = 坐标
```

---

# 3. models/ 文件夹

## 3.1 unfitted_model.rds

### 是什么？

未拟合的 Hmsc 模型对象。

### 用途

用于检查模型结构是否正确：

```text
Y 是否进入模型
XFormula 是否正确
随机效应是否创建
TrData/phylogeny 是否加入
```

### 用户是否需要打开？

普通用户一般不用打开。高级用户可在 R 里读取：

```r
m <- readRDS("models/unfitted_model.rds")
```

---

## 3.2 unfitted_models.RData

### 是什么？

未拟合模型的 RData 格式保存。

### 用途

和官方 Hmsc 脚本工作流更接近，便于 R 用户继续分析。

---

## 3.3 hmsc_model.rds

### 是什么？

拟合完成后的 Hmsc 模型对象。

### 用途

这是最重要的模型对象，可用于：

```text
重新计算结果
画更多图
继续做预测
检查后验样本
```

### R 读取方式

```r
m <- readRDS("models/hmsc_model.rds")
```

---

## 3.4 models_thin_*_samples_*_chains_*.RData

### 是什么？

拟合模型的 RData 格式，文件名中记录 MCMC 参数。

例如：

```text
models_thin_1_samples_100_chains_2.RData
```

表示：

```text
thin = 1
samples = 100
chains = 2
```

### 用途

方便知道模型是用什么 MCMC 设置跑的。

---

# 4. results/ 文件夹

## 4.1 model_structure.txt

### 是什么？

模型结构文本说明。

### 里面有什么？

可能包括：

```text
响应变量分布
公式
随机效应结构
样方数
物种数
性状/系统发育是否使用
```

### 怎么看？

检查是否与你界面设置一致。

---

## 4.2 predicted_values.rds

### 是什么？

模型预测值对象。

### 行列含义

通常对应：

```text
行 = 样方
列 = 物种
值 = 模型预测响应
```

对于 `probit`，预测值可理解为出现概率或潜在尺度转换后的预测，具体取决于 Hmsc 输出对象结构。

### 用途

用于模型拟合评估和进一步预测分析。

---

## 4.3 model_fit_explanatory.txt

### 是什么？

解释性模型拟合结果。

### 含义

评价模型对训练数据的解释能力。

### 常见指标

可能包括：

```text
AUC
Tjur R2
RMSE
R2
```

具体指标取决于 `distr`。

### 行列含义

如果导出为表格，通常：

```text
行 = 物种
列 = 拟合指标
```

例如：

| species | AUC | TjurR2 |
|---|---:|---:|
| sp1 | 0.82 | 0.31 |
| sp2 | 0.76 | 0.22 |

### 注意

这是训练数据上的拟合能力，通常比交叉验证结果更乐观。

---

## 4.4 model_fit_cross_validation.txt

### 是什么？

交叉验证预测结果。

### 行列含义

通常：

```text
行 = 物种
列 = 预测指标
```

### 怎么解释？

比解释性拟合更能反映模型对新数据的预测能力。

### 注意

1. 小样本结果波动大。
2. 稀有物种的预测指标可能不稳定。
3. 不同 nfolds 会影响结果。

---

## 4.5 WAIC.txt

### 是什么？

WAIC 模型比较指标。

### 怎么解释？

通常用于比较多个候选模型：

```text
WAIC 越低，模型相对支持度越高
```

### 注意

1. 单个模型的 WAIC 不太有解释力。
2. 多模型比较时才更有意义。
3. WAIC 不代表生态机制一定正确。

---

## 4.6 MCMC_convergence.txt

### 是什么？

MCMC 收敛诊断文本结果。

### 里面可能有什么？

```text
PSRF / Rhat
ESS
后验样本摘要
链间比较
```

### 怎么解释？

常见原则：

```text
Rhat / PSRF 接近 1 更好
ESS 越大越好
traceplot 应该混合良好
```

### 注意

正式论文不能只说模型跑完，要说明收敛诊断结果。

---

## 4.7 parameter_estimates.txt

### 是什么？

参数估计摘要。

### 包括什么？

可能包括：

```text
Beta：环境响应
Gamma：性状对环境响应的影响
Omega：物种残差关联
```

### 怎么看？

应结合 `tables/` 中的 CSV 和 `plots/` 中的 PDF 图一起解释。

---

## 4.8 species_associations.rds

### 是什么？

物种残差关联对象。

### 行列含义

如果转换成矩阵：

```text
行 = 物种
列 = 物种
值 = 残差关联
```

### 重要警告

Omega 残差关联不等于直接种间作用。  
它可能来自：

```text
未测环境变量
空间结构
真实相互作用
采样偏差
模型结构不足
```

---

# 5. tables/ 文件夹

## 5.1 data_dimensions.csv

### 是什么？

输入数据维度表。

### 行列含义

通常记录：

```text
Y 的样方数
Y 的物种数
XData 的变量数
TrData 的物种数和性状数
```

### 用途

检查输入数据是否读对。

---

## 5.2 species_summary.csv

### 是什么？

物种响应矩阵摘要。

### 行列含义

通常：

```text
行 = 物种
列 = 摘要指标
```

可能包括：

```text
出现次数
出现比例
平均丰度
最小值
最大值
缺失值数量
```

### 怎么用？

识别稀有物种、全 0 物种、异常物种。

---

## 5.3 environment_summary.csv

### 是什么？

环境变量摘要。

### 行列含义

通常：

```text
行 = 环境变量
列 = 摘要指标
```

可能包括：

```text
均值
标准差
最小值
最大值
缺失值数量
变量类型
```

### 怎么用？

检查环境变量是否异常、是否有缺失值、是否需要标准化。

---

## 5.4 model_fit_explanatory_*.csv

### 是什么？

解释性模型拟合表格版。

### 行列含义

```text
行 = 物种
列 = 拟合指标
```

对 presence/absence 数据，常见指标：

```text
AUC
TjurR2
```

对连续数据，可能有：

```text
RMSE
R2
```

---

## 5.5 model_fit_cross_validation_*.csv

### 是什么？

交叉验证结果表。

### 行列含义

```text
行 = 物种
列 = 预测指标
```

### 怎么写论文？

可以报告：

```text
平均预测能力
不同物种预测能力范围
哪些物种预测较好/较差
```

---

## 5.6 parameter_estimates_Beta_*.csv

### 是什么？

Beta 环境响应参数表。

### 行列含义

不同导出格式可能略有不同，但核心是：

```text
物种
环境变量
后验均值
支持度/显著性支持
不确定性
```

### 怎么解释？

如果某物种对 pH 的 Beta 为正：

```text
pH 越高，该物种出现概率/响应值越高
```

如果为负：

```text
pH 越高，该物种出现概率/响应值越低
```

### 注意

1. 参数尺度取决于 distr。
2. 变量是否标准化会影响参数大小。
3. 支持度高不等于生态效应一定大。

---

## 5.7 parameter_estimates_Gamma_*.csv

### 是什么？

Gamma 性状参数表。

### 行列含义

通常涉及：

```text
性状
环境响应参数
后验均值
支持度
不确定性
```

### 怎么解释？

Gamma 表示性状是否能解释物种的环境响应差异。

例如：

```text
height_mm 对 pH 响应的 Gamma 为正
```

可以理解为：

```text
较高的物种可能对 pH 增加有更正向的响应
```

### 注意

Gamma 解释比 Beta 更抽象，需要结合性状生态意义。

---

## 5.8 variance_partitioning_*.csv

### 是什么？

方差分解结果。

### 行列含义

通常：

```text
行 = 物种
列 = 解释组
值 = 该解释组解释的比例
```

解释组可能包括：

```text
环境变量组
随机效应
空间效应
```

### 怎么解释？

某物种在环境变量组上的值高，说明该物种响应较多由环境解释。

---

## 5.9 Omega_*_*.csv

### 是什么？

物种残差关联矩阵。

### 行列含义

```text
行 = 物种 A
列 = 物种 B
单元格 = 物种 A 和物种 B 的残差关联
```

### 对角线

```text
物种和自身的关联，一般不是重点
```

### 正值

```text
控制环境后，两个物种仍倾向一起出现
```

### 负值

```text
控制环境后，两个物种倾向不一起出现
```

### 注意

不能直接把正值写成互利，不能直接把负值写成竞争。

---

# 6. plots/ 文件夹

## 6.1 MCMC_traceplots.pdf

### 是什么？

MCMC 链轨迹图。

### 怎么看？

好的 traceplot 通常：

```text
链混合良好
没有明显趋势
不同链在同一范围内波动
像稳定的毛毛虫
```

不好的 traceplot：

```text
单调上升/下降
链之间分离
突然跳跃
长期停在某一区域
```

---

## 6.2 model_fit_explanatory_vs_predictive.pdf

### 是什么？

解释性拟合与预测性拟合对比图。

### 怎么看？

如果解释性拟合很好，但交叉验证很差：

```text
可能过拟合
模型对训练数据解释好，但预测新数据弱
```

---

## 6.3 Beta_plot.pdf

### 是什么？

环境响应参数图。

### 行列含义

通常：

```text
行/列 = 物种和环境变量的组合
颜色/点/符号 = 参数方向和支持度
```

### 怎么看？

重点看：

```text
哪些环境变量影响最多物种
哪些物种响应最强
正负方向是否符合生态预期
```

---

## 6.4 Gamma_plot.pdf

### 是什么？

性状影响参数图。

### 怎么看？

看哪些性状解释了物种对环境的响应差异。

---

## 6.5 variance_partitioning.pdf

### 是什么？

方差分解图。

### 怎么看？

看每个物种的变异主要由：

```text
环境变量
空间结构
随机效应
残差
```

中的哪一部分解释。

---

## 6.6 Omega_associations_*.pdf

### 是什么？

物种残差关联图。

### 怎么看？

通常颜色表示正/负关联，强度表示关联大小。

### 注意

图里强关联需要谨慎解释，最好结合生态知识和未测变量可能性。

---

## 6.7 predictions_environmental_gradients.pdf

### 是什么？

环境梯度预测图。

### 行列含义

通常：

```text
横轴 = 环境变量梯度
纵轴 = 预测响应
不同线/面板 = 不同物种
```

### 怎么看？

看物种响应随环境变量如何变化：

```text
增加
减少
单峰
无明显变化
```

### 注意

不能外推到观测数据范围之外。

---

# 7. output ZIP

## 是什么？

当点击下载结果时，程序会把本次结果文件夹打包。

### 用途

```text
保存完整结果
发给合作者
作为论文补充材料
日后复现
```

### 建议

每次正式分析都保存 output ZIP，并记录对应数据版本。
